# Sovereignly Platform Protocol (SPP)

Core primitives:

TENANT
MACHINE
WORKFLOW
AGENT
EVENT
POLICY

Example endpoints:

POST /v1/tenants
POST /v1/machines
POST /v1/workflows/trigger

All operations require authentication and policy validation.