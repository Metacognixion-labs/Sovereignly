app            = "sovereignly"
primary_region = "iad"

[build]
  dockerfile = "apps/cloud/Dockerfile"

[env]
  PORT             = "8787"
  NODE_ENV         = "production"
  WORKER_POOL_SIZE = "0"
  LOG_LEVEL        = "minimal"
  ANCHOR_TIER      = "starter"
  NODE_ROLE        = "cluster"
  CLUSTER_ID       = "us-east"
  NODE_REGION      = "us-east"

[http_service]
  internal_port        = 8787
  force_https          = true
  auto_stop_machines   = false
  auto_start_machines  = true
  min_machines_running = 1

  [http_service.concurrency]
    type = "requests"
    hard_limit = 250
    soft_limit = 200

  [[http_service.checks]]
    grace_period = "30s"
    interval     = "15s"
    method       = "GET"
    path         = "/_sovereign/health"
    protocol     = "http"
    timeout      = "5s"

[[restart]]
  policy      = "on-failure"
  max_retries = 5

[[vm]]
  size   = "shared-cpu-1x"
  memory = "2048mb"

[[mounts]]
  source      = "sovereignly_data"
  destination = "/data"
